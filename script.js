const observer=new IntersectionObserver((entries)=>entries.forEach((entry,i)=>{if(entry.isIntersecting){setTimeout(()=>entry.target.classList.add('visible'),i*65);observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const glow=document.querySelector('.cursor-glow');window.addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'});
const navbar=document.querySelector('.navbar');const updateNavbar=()=>navbar.classList.toggle('scrolled',scrollY>40);updateNavbar();addEventListener('scroll',updateNavbar,{passive:true});
const progress=document.querySelector('.scroll-progress');const updateProgress=()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${max?scrollY/max*100:0}%`};updateProgress();addEventListener('scroll',updateProgress,{passive:true});addEventListener('resize',updateProgress);
document.querySelectorAll('.btn').forEach(button=>{button.addEventListener('pointermove',e=>{if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;const r=button.getBoundingClientRect();button.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.12}px,${(e.clientY-r.top-r.height/2)*.16}px)`});button.addEventListener('pointerleave',()=>button.style.transform='')});
document.querySelectorAll('.skill-card').forEach(card=>{card.addEventListener('pointermove',e=>{if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(800px) rotateX(${-y*5}deg) rotateY(${x*6}deg) translateY(-7px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});
document.querySelectorAll('.nav-link').forEach(link=>link.addEventListener('click',()=>{const nav=document.querySelector('.navbar-collapse');bootstrap.Collapse.getInstance(nav)?.hide()}));
const host=document.getElementById('three-canvas'),scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(55,innerWidth/innerHeight,.1,100),renderer=new THREE.WebGLRenderer({alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(innerWidth,innerHeight);host.appendChild(renderer.domElement);camera.position.z=8;
const group=new THREE.Group();scene.add(group);const core=new THREE.Mesh(new THREE.IcosahedronGeometry(1.25,1),new THREE.MeshBasicMaterial({color:0x67ecff,wireframe:true,transparent:true,opacity:.52}));group.add(core);const ring=new THREE.Mesh(new THREE.TorusGeometry(1.75,.018,8,80),new THREE.MeshBasicMaterial({color:0x9d7bff,transparent:true,opacity:.55}));ring.rotation.x=1.1;group.add(ring);const particles=new THREE.BufferGeometry(),count=250,points=new Float32Array(count*3);for(let i=0;i<points.length;i++)points[i]=(Math.random()-.5)*18;particles.setAttribute('position',new THREE.BufferAttribute(points,3));scene.add(new THREE.Points(particles,new THREE.PointsMaterial({color:0x67ecff,size:.027,transparent:true,opacity:.65})));
function animate(){requestAnimationFrame(animate);group.rotation.y+=.003;group.rotation.x+=.001;group.position.y=Math.sin(Date.now()*.001)*.2;renderer.render(scene,camera)}animate();addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});
